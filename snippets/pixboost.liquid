{%- if image != blank -%}
    {%- if scale == blank and size == blank -%}
        {%- assign original_img_url = image | url_encode | replace: '_%7Bwidth%7Dx.', '_{width}x.' -%}
    {%- endif -%}

    {%- if scale == blank and size != blank -%}
        {%- assign original_img_url = image | img_url: size | url_encode -%}
    {%- endif -%}

    {%- if size != blank and scale != blank -%}
        {%- assign original_img_url = image | img_url: size, scale: scale | url_encode  -%}
    {%- endif -%}

    https://pixboost.com/api/2/img/{{original_img_url}}/optimise?auth=MTg4MjMxMzM3MA__
{%- endif -%}